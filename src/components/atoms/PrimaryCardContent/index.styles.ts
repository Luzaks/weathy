import styled from "styled-components";
import { Box } from "@mui/material";

const StyledCardContent = styled(Box)({
  padding: 5,
  marginBottom: 5,
  background: "linear-gradient(55deg, #cb2d3e 90%, #ef473a 90%)",
  borderRadius: 4,
  textAlign: "center",
  boxShadow: "10px 10px 3px 10px rgba(240,227,227,0.41)",
});
export default StyledCardContent;
