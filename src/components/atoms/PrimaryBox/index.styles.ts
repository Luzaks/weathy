import styled from "styled-components";
import { Box } from "@mui/material";

const StyledBox = styled(Box)({
  boxShadow: "0px 0px 3px 3px rgba(240,227,227,0.41)",
  boxSizing: "border-box",
});
export default StyledBox;
